from p5 import *

img = None

def setup():
    global img
    size(720, 400)
    img = load_image("./map_sprites/tile192.jpg")

def draw():
   
    background(0)
    image(img, (0, 0))
    #image(img, (0, height / 2), (img.width / 2, img.height / 2))

if __name__ == '__main__':
    run()

